1. The only dependency to be downloaded is JSoup. The downloaded jar file must be added to the path of the project.

2. The code will not require to execute any build command.

3. Running the web crawler driver will display the output of the program.}
